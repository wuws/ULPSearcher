# ULPSearcher

ULPSearcher is a Python command-line tool designed to quickly filter large combo lists (such as email:password or user:password formats) using keyword matching.

## Features

- Fast filtering of large combo files
- Simple command-line usage
- Lightweight and easy to run
